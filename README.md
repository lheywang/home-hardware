# home-hardware

Repo for the home-hardware website for the tutorials and so on !

## Dependencies

```sh
npm install smol-toml
```

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command                   | Action                                           |
| :------------------------ | :----------------------------------------------- |
| `npm install` | Installs dependencies                            |
| `npm run dev` | Starts local dev server at `localhost:4321` |
| `npm run build` | Build your production site to `./dist/` |
| `npm run preview` | Preview your build locally, before deploying     |
| `npm run astro ...` | Run CLI commands like `astro add` , `astro check` |
| `npm run astro -- --help` | Get help using the Astro CLI                     |

## Contribute : 

All of this repo is open-sourced, therefore it's easy to contribute. You have two options : 
* Else you're used to Git ecosystem, and you may submit a PR to the repo (I'll review and merge them). That's the preferred way.
* Else you're not, and i that case you could imagine going to the discord server, and send me a PM with your content. For your first submission, you may need to provide me a profile picture !
